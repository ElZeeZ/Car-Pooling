{
  "name": "routely",
  "version": "0.1.0",
  "private": true,
  "description": "Full-stack backbone for the Routely web application.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "predev:lan": "node scripts/print-lan-info.js",
    "dev:lan": "concurrently -n server,client -c blue,green \"npm run dev --workspace server\" \"npm run dev --workspace client -- --host 0.0.0.0\"",
    "dev:vpn": "node scripts/start-vpn-dev.js",
    "dev:stop": "node scripts/stop-dev-ports.js",
    "dev:client": "npm run dev --workspace client",
    "dev:server": "npm run dev --workspace server",
    "vpn:serve": "node scripts/start-vpn-dev.js --serve-only",
    "vpn:status": "node scripts/start-vpn-dev.js --status",
    "vpn:stop": "node scripts/start-vpn-dev.js --stop",
    "start": "npm run start --workspace server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
